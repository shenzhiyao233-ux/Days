# 测试与验收

## 自动测试

`./gradlew testDebugUnitTest` 覆盖今天、明天、昨天、是否计入今天、跨月、跨年、闰年、每年重复和正数日。

## iQOO / vivo OriginOS 真机重点清单

1. 从 OriginOS 桌面组件库添加两个“日子”组件，分别绑定不同事件。
2. 使用系统照片选择器选图；确认 App 未申请照片、文件或网络权限。
3. 给不同 Widget 选择不同照片，强制停止 App 后确认两张图仍独立显示。
4. 分别检查 2×1、2×2，并拖动缩放 Widget，确认图片裁切、文字和圆角没有异常。
5. 调节 0%、20%、40%、60%、70% 遮罩，分别检查自动、白色、黑色文字。
6. 切换系统深浅色；检查系统背景、纯色、半透明和照片背景。
7. 重启手机，确认事件、绑定及照片仍显示。
8. 手动把日期调到第二天，再恢复自动日期；确认数字刷新且照片保留。
9. 切换时区；确认本地日期和 Widget 数字同步改变。
10. 在 OriginOS 最近任务中清理后台，等待桌面重载，确认 Widget 仍正常。
11. 删除内部背景文件（仅测试构建可用 adb 操作），确认 Widget 回退默认背景而不崩溃。
12. 删除事件，确认对应 Widget 显示“事件已删除”；其他 Widget 不受影响。

注意：部分 OriginOS 版本会冻结第三方应用。若跨日广播被厂商策略延迟，24 小时系统更新周期仍会兜底；建议同时观察实际午夜刷新时刻。
